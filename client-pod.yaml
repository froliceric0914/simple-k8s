apiVersion: v1
kind: Pod
metadata:
  name: client-Pod
  labels:
    component: web
spec:
  container:
    - name: client
      image: stephengrider/multi-client
      ports:
        - containerPort: 3000
